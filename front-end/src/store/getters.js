export const user = state => state.user

export const myGames = state => {
  return state.myGames
}

export const availableGames = state => {
  return state.availableGames
}

export const activeGames = state => {
  return state.activeGames
}
